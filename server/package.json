{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "weather app",
  "main": "server.js",
  "scripts": {
    "start": "npm run build-react && node server.js",
    "build-react": "npm --prefix ../client run build"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "express": "^4.10.*"
  }
}
